let decoration = {
    color:'red',
    radius: 10
};

// for(const prop in decoration){
//     console.log(prop);  // it only give the property not the values;
// }

// to get the value from the object you can use this:

// for(const prop in decoration){
//     console.log(decoration[prop]);
// }



// for in loops in the array:

const arr = ['hello', 1, 'javascript','true'];

for(let x in arr){
    console.log(arr[x])
}


